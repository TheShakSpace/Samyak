# =============================================================================
# Agentic Task & Management – Backend Environment (Remote Team Productivity)
# Copy to .env and fill in values. Never commit .env.
#   cp env.example .env
# =============================================================================

# --- Gemini (required for Voice Agent) ---
# Get key: https://aistudio.google.com/ → Get API key
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.5-flash

# --- Firebase (backend only) ---
# For Firestore: download SERVICE ACCOUNT JSON from Firebase Console →
#   Project settings → Service accounts → Generate new private key.
# Put path to that JSON file here (relative to backend/ or absolute).
GOOGLE_APPLICATION_CREDENTIALS=path/to/firebase-service-account.json
# Optional: set if not using service account JSON (e.g. project ID for emulator)
FIREBASE_PROJECT_ID=samyak-ai-7596a

# --- Backend server ---
BACKEND_BASE_URL=http://localhost:8000
# Optional: host/port for uvicorn
# HOST=0.0.0.0
# PORT=8000

# --- Slack webhooks (optional) ---
# #samayak-project-tasks: when someone assigns tasks to you
WEBHOOK_URL_SLACK_TASKS=https://hooks.slack.com/services/YOUR/WORKSPACE/TASKS_WEBHOOK
# #samyak: agent (Gemini) sends detailed task breakdown, deadline, how to do it
WEBHOOK_URL_SLACK_AGENT=https://hooks.slack.com/services/YOUR/WORKSPACE/AGENT_WEBHOOK
# Bot User OAuth Token (from Slack app OAuth & Permissions; optional, for future API use)
# SLACK_BOT_TOKEN=xoxb-...
# Single webhook fallback (if you only use one channel)
# WEBHOOK_URL_DISCORD=...
# WEBHOOK_URL_TEAMS=...

# --- Google OAuth (Gmail, Calendar) ---
# From Google Cloud Console → Credentials → your OAuth 2.0 Client ID (Web application)
GOOGLE_OAUTH_CLIENT_ID=your_client_id.apps.googleusercontent.com
GOOGLE_OAUTH_CLIENT_SECRET=your_client_secret

# --- Email (optional; for reminders & summaries) ---
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_ADDRESS=
EMAIL_PASSWORD=

# --- Auth (optional; if backend verifies JWT/Firebase tokens) ---
# JWT_SECRET=your-jwt-secret
# FIREBASE_AUTH_EMULATOR_HOST=  # only for local emulator

# --- Legacy (deprecated after Gemini migration) ---
# LLM_PROVIDER=gemini
# OPENAI_API_KEY=  # not needed when using Gemini
